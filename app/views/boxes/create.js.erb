<script>
  const boxesContainer = document.getElementById('boxes-container');
  boxesContainer.innerHTML = '<%= j(render(partial: 'boxes/form', locals: { image: @image, box: Box.new })) %>';
</script>
